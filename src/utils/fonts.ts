import { Quicksand, Righteous } from 'next/font/google'

export const quicksandLight = Quicksand({
  subsets: ['latin'],
  weight: '300'
})

export const quicksand = Quicksand({
  subsets: ['latin'],
  weight: '500'
})

export const quicksandBolder = Quicksand({
  subsets: ['latin'],
  weight: '600'
})

export const righteous = Righteous({
  subsets: ['latin'],
  weight: '400'
})